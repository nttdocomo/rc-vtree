branches:
  only:
  - master

language: node_js

notifications:
  email:
  - nttdocomo.ouyi@gmail.com

node_js:
  - 'stable'

before_install:
  - |
    if ! git diff --name-only $TRAVIS_COMMIT_RANGE | grep -qvE '(\.md$)|(^(docs|examples))/'
    then
      echo "Only docs were updated, stopping build process."
      exit
    fi

jobs:
  include:
    - stage: "Tests"
      name: "Unit Tests"
      script:
        - |
          npm test -- --coverage && \
          bash <(curl -s https://codecov.io/bash)
    - stage: deploy
      name: "Deploy"
      script:
        - |
          npm run semantic-release
      env:
        - GH_TOKEN=f0456a7be6dd989d8f4fba829b87e944fc1cff2b
        - NPM_TOKEN=fb33e1b9-640b-4ffb-9494-0a2bcb95209